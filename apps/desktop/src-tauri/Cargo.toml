[package]
name = "schoolquest"
version = "0.1.0"
description = "SchoolQuest desktop app"
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "schoolquest_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
# Native file picker for syllabus upload — the desktop app's reason to exist.
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]
